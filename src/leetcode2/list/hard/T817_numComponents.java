package leetcode2.list.hard;

import leetcode2.utils.ListNode;

public class T817_numComponents {
    /**
     * 链表组件
     *
     * 给定一个链表（链表结点包含一个整型值）的头结点 head。
     *
     * 同时给定列表 G，该列表是上述链表中整型值的一个子集。
     *
     * 返回列表 G 中组件的个数，这里对组件的定义为：链表中一段最长连续结点的值
     * （该值必须在列表 G 中）构成的集合。
     * */
    private static int numComponents(ListNode head, int[] G) {
        return -1;
    }
}
